# ── SECURITY: Vaultwarden ──

services:

  vaultwarden:
    image: ${VAULTWARDEN_IMAGE}:${VAULTWARDEN_VERSION}
    restart: unless-stopped
    ports:
      - "${VAULTWARDEN_PORT:-8087}:80"
    volumes:
      - ${DATA_ROOT}/vaultwarden/data:/data
    environment:
      SIGNUPS_ALLOWED: "${VAULTWARDEN_SIGNUPS_ALLOWED:-false}"
    user: "1000:1000"
    networks:
      - homelab
