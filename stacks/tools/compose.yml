# ── TOOLS: Excalidraw ──
# docs/stacks/tools.md

services:
  excalidraw:
    image: ${EXCALIDRAW_IMAGE}:${EXCALIDRAW_VERSION}
    restart: unless-stopped
    ports:
      - "${EXCALIDRAW_PORT:-8092}:80"
    networks:
      - homelab
